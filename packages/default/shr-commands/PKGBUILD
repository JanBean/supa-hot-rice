pkgname=shr-commands
pkgver=1.0
pkgrel=2
pkgdesc='Supa Hot Rice Terminal Commands'
arch=(any)
license=(None)
depends=(
    neovim          # text editor
    wget            # file downloader
    unzip           # Extract zip files
    xclip           # Clipboard tool
    stow            # Manage dotfiles
    python-pywal16  # color sheme generation
    gum             # shell shit
    # less
)